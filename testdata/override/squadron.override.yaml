version: "2.3"

squadron:
  storefinder:
    frontend:
      builds:
        default:
          # append new element to array
          build_arg:
            - "bar=baz"
            - "baz=baz"
    # override backend
    backend: ~
