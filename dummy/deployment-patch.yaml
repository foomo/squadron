
spec:
  template:
    metadata:
      labels:
        {{ .PatchedLabelName }}: "true"
    spec:
      containers:
      - name: {{ .ContainerName }}
        image: {{ .Image }}
        command:
        args:
        livenessProbe:
        readinessProbe:
        volumeMounts:
          - mountPath: {{ .MountPath }}
            name: "{{ .ContainerName }}-patch-mount"
      volumes:
        - name: "{{ .ContainerName }}-patch-mount"
          hostPath:
            path: {{ .HostPath }}